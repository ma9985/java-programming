package day44_custom_classes;

public class App {
    String name;  //means null by default
    double version; //0.0 by default
// open method is using name and version
    void open() {
        System.out.println("rabilDeen " + name +" app - version = " + version );
    }
}
